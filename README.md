A Random joke generator utilising 4 different API's

Requirements: Composer, Npm

How to run: Pull from git and run from terminal;
```
composer install
npm install && npm run dev
mv .env.example .env
php artisan key:generate
```
